server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optionally proxy API requests to backend. Uncomment and set backend host if needed.
  # location /api/ {
  #   proxy_pass http://backend:5000;
  # }

  error_page 500 502 503 504 /50x.html;
}
